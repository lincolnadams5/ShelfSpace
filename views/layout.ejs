<!--
    layout.ejs
    Base layout wrapper used by all pages.
    Contains the main HTML structure: <head>, CSS/JS links, and the navbar partial.
    Every page's content is injected inside <%- body %>.
-->
